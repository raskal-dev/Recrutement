global:
  resolve_timeout: 5m

route:
  receiver: 'webhook-receiver'
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 1h

receivers:
  - name: 'webhook-receiver'
    webhook_configs:
      - url: 'http://backend:3000/alert'
